<view class="container">
  <view class="movie" wx:for="{{ movies }}">
    <image class="poster" src="/images/{{ item.poster }}"/>
    <view class="movie-info">
      <view class="title">{{ item.title }}</view>
      <view class="genre">类型：{{ item.genre }}</view>
    </view>
  </view>
</view>